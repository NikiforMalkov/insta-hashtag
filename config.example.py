userConfig = {
    "login": "login",
    "password": "password",
}

databaseConfig = {
   # пример соединения 'dialect+driver://user:pass@host:port/db'
    "connection": "mysql+pymysql://root:@localhost/insta-hashtag",
}